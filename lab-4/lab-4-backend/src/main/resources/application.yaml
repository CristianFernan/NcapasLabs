spring:
  application:
    name: app

  datasource:
    url: jdbc:postgresql://localhost:5432/leases_credits
    username: postgres
    password: tu_password

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  web:
    cors:
      allowed-origins: http://localhost:5173

security:
  jwt:
    secret-key: "Zm9vYmFyYmF6cXV4c2VjcmV0a2V5MTIzNDU="
    expiration-time: 3600000

